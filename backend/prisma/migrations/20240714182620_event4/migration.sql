BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[bookings] ADD [singleTicketSlots] INT NOT NULL CONSTRAINT [bookings_singleTicketSlots_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
