BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[role_requests] ADD [isDeleted] BIT NOT NULL CONSTRAINT [role_requests_isDeleted_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
